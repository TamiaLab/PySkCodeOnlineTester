# Required metadata
sonar.projectKey=django:skcode-online-tester
sonar.projectName=Online PySkCode Testing Website
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=apps,skcodeonlinetester

# Language
sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# File exclusions
sonar.exclusions=**/tests/**,**/tests.py,**/migrations/**
